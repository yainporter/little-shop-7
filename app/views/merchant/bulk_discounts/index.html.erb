<h1>All of <%= @merchant.name %>'s Bulk Discounts</h1>
<%= link_to 'Create New Discount', new_merchant_bulk_discount_path(@merchant.id), data: { turbo_method: :get } %>
  <% @merchant.bulk_discounts.each do |discount| %>
    <ul id="discount-<%= discount.id %>">
      <li><%= discount.name %></li>
      <li>Discount: <%= number_to_percentage(discount.percentage, precision: 0) %></li>
      <li>Quantity Threshold: <%= discount.quantity_threshold %></li>
      <li><%= button_to "Delete Discount", merchant_bulk_discount_path(@merchant.id, discount.id), method: :delete %></li>
    </ul>
  <% end %>

